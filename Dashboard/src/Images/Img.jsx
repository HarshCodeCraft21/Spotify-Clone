export const Img = ({ src, alt, className }) => {
    return (
        <>
            <img src={[
                '../../Public/Images/Logo.png'
            ]}
                alt={alt} className={className} />
        </>
    )
}
